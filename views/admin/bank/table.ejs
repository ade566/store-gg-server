<table id="example2" class="table table-bordered table-hover">
  <thead>
  <tr>
    <th>No</th>
    <th>Nama</th>
    <th>Bank</th>
    <th>No Rekening</th>
    <th>Aksi</th>
  </tr>
  </thead>
  <tbody>
    <% data.forEach((e, i) => { %>
      <tr>
        <td><%= i + 1 %></td>
        <td><%= e.name %> </td>
        <td><%= e.bankName %> </td>
        <td><%= e.noRekening %> </td>
        <td>
          <form action="/bank/delete/<%= e._id %>?_method=DELETE" method="post">
            <a href="/bank/edit/<%= e._id %>" class="btn btn-sm btn-warning">
              <i class="fas fa-pencil-alt"></i>
              Ubah
            </a>
            <button href="" class="btn btn-sm btn-danger">
              <i class="fas fa-trash"></i>
              Hapus
            </button>
          </form>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>